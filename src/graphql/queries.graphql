query GetCharacters {
  characters {
    results {
      species
      name
      id
      image
    }
  }
}

query GetCharacter($id: ID!) {
  character(id: $id) {
    name
    image
    status
  }
}
